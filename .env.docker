#.env.docker (for Compose on Postgres):

APP_ENV=localhost

POSTGRES_USER=app
POSTGRES_PASSWORD=app
POSTGRES_DB=appdb
DATABASE_URL=postgresql+psycopg://app:app@db:5432/appdb
OTHER_API_BASE=http://other-api:8004

#JWT settings (used in my main.py via os.getenv)
JWT_SECRET=AdminJoshFYP_Secret
JWT_ALG=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

#Model Path inside the container
MODEL_PATH=models/tablet_model.keras